modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Server"

[[workflows.workflow]]
name = "Server"
author = "agent"

[workflows.workflow.metadata]
agentRequireRestartOnSave = false

[[workflows.workflow.tasks]]
task = "packager.installForAll"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "npm install react react-dom react-router-dom axios bootstrap cors express vite @vitejs/plugin-react && npx vite build && node server.js"
waitForPort = 5000

[deployment]
run = ["sh", "-c", "npm install react react-dom react-router-dom axios bootstrap cors express vite @vitejs/plugin-react && npx vite build && node server.js"]

[[ports]]
localPort = 5000
externalPort = 80
